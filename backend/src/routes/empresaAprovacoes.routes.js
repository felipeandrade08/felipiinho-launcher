const express=require('express');const router=express.Router();const {pool}=require('../config/database');const {exigirAutenticacao,exigirAdmin}=require('../middlewares/autenticacao');
function slugify(t){return String(t||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'');}
router.post('/',exigirAutenticacao,async(req,res)=>{try{const usuarioId=req.usuario.id;const {nome,localizacao,responsavel,email,discord,instagram,site,data_fundacao,descricao,logo_url,capa_url}=req.body||{};if(!nome||!responsavel||!email)return res.status(400).json({sucesso:false,mensagem:'Nome, responsável e e-mail são obrigatórios.'});const slug=slugify(nome);const [[ex]]=await pool.query('SELECT id FROM empresas WHERE LOWER(nome)=LOWER(?) LIMIT 1',[nome]);if(ex)return res.status(409).json({sucesso:false,mensagem:'Já existe uma empresa com este nome.'});const [[p]]=await pool.query("SELECT id FROM solicitacoes_empresas WHERE usuario_id=? AND status='pendente' LIMIT 1",[usuarioId]);if(p)return res.status(409).json({sucesso:false,mensagem:'Você já possui uma solicitação de empresa aguardando análise.'});const [r]=await pool.query(`INSERT INTO solicitacoes_empresas(usuario_id,nome,slug,responsavel,email,localizacao,discord,instagram,site,data_fundacao,descricao,logo_url,capa_url) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)`,[usuarioId,nome,slug,responsavel,email,localizacao||null,discord||null,instagram||null,site||null,data_fundacao?`${data_fundacao}-01-01`:null,descricao||null,logo_url||null,capa_url||null]);res.status(201).json({sucesso:true,mensagem:'Cadastro da empresa enviado para análise.',dados:{id:r.insertId,status:'pendente'}});}catch(e){console.error(e);res.status(500).json({sucesso:false,mensagem:'Erro ao enviar solicitação de empresa.'});}});
router.get('/',exigirAutenticacao,exigirAdmin,async(req,res)=>{try{const [rows]=await pool.query(`SELECT s.*,u.nome AS solicitante_nome,u.email AS solicitante_email FROM solicitacoes_empresas s JOIN usuarios u ON u.id=s.usuario_id WHERE s.status='pendente' ORDER BY s.criado_em ASC`);res.json({sucesso:true,dados:rows});}catch(e){console.error(e);res.status(500).json({sucesso:false,mensagem:'Erro ao carregar solicitações de empresas.'});}});
router.patch('/:id/aprovar',exigirAutenticacao,exigirAdmin,async(req,res)=>{const c=await pool.getConnection();try{await c.beginTransaction();const [[s]]=await c.query("SELECT * FROM solicitacoes_empresas WHERE id=? AND status='pendente' FOR UPDATE",[req.params.id]);if(!s){await c.rollback();return res.status(404).json({sucesso:false,mensagem:'Solicitação não encontrada ou já analisada.'});}const [[sl]]=await c.query('SELECT id FROM empresas WHERE slug=? LIMIT 1',[s.slug]);if(sl){await c.rollback();return res.status(409).json({sucesso:false,mensagem:'Já existe uma empresa com este endereço.'});}const [emp]=await c.query(`INSERT INTO empresas(nome,slug,descricao,logo_url,capa_url,localizacao,motoristas,caminhoes,data_fundacao,discord,instagram,site,pontuacao_ranking,posicao_ranking,destaque) VALUES(?,?,?,?,?,?,0,0,?,?,?,?,0,NULL,0)`,[s.nome,s.slug,s.descricao,s.logo_url,s.capa_url,s.localizacao,s.data_fundacao,s.discord,s.instagram,s.site]);const [conta]=await c.query("INSERT INTO contas(nome,tipo,email_contato,status) VALUES(?,'empresa',?,'ativa')",[s.nome,s.email]);await c.query("UPDATE empresas SET conta_id=? WHERE id=?",[conta.insertId,emp.insertId]);await c.query("INSERT INTO conta_membros(conta_id,usuario_id,papel,status) VALUES(?,?,'proprietario','ativo')",[conta.insertId,s.usuario_id]);const [[plano]]=await c.query("SELECT id FROM planos WHERE codigo='empresa' AND ativo=TRUE LIMIT 1");if(plano)await c.query("INSERT INTO assinaturas(conta_id,plano_id,status,inicio_em,fim_em,trial_inicio,trial_fim,trial_usado) VALUES(?,?,'trial',NOW(),DATE_ADD(NOW(),INTERVAL 7 DAY),NOW(),DATE_ADD(NOW(),INTERVAL 7 DAY),FALSE)",[conta.insertId,plano.id]);await c.query("UPDATE solicitacoes_empresas SET status='aprovada',analisado_por=?,analisado_em=NOW(),empresa_id=? WHERE id=?",[req.usuario.id,emp.insertId,s.id]);await c.commit();res.json({sucesso:true,mensagem:'Empresa aprovada com sucesso.',dados:{id:emp.insertId,conta_id:conta.insertId}});}catch(e){await c.rollback();console.error(e);res.status(500).json({sucesso:false,mensagem:'Erro ao aprovar empresa.'});}finally{c.release();}});
router.patch('/:id/rejeitar',exigirAutenticacao,exigirAdmin,async(req,res)=>{try{const motivo=String(req.body?.motivo||'').trim().slice(0,500)||null;const [r]=await pool.query("UPDATE solicitacoes_empresas SET status='rejeitada',motivo_recusa=?,analisado_por=?,analisado_em=NOW() WHERE id=? AND status='pendente'",[motivo,req.usuario.id,req.params.id]);if(!r.affectedRows)return res.status(404).json({sucesso:false,mensagem:'Solicitação não encontrada ou já analisada.'});res.json({sucesso:true,mensagem:'Solicitação rejeitada.',dados:null});}catch(e){console.error(e);res.status(500).json({sucesso:false,mensagem:'Erro ao rejeitar solicitação.'});}});module.exports=router;